// This hook was replaced by useScrollPreservation.js
// Keeping this file to avoid import errors, but it's no longer used

export const useScrollPosition = () => {
  return {};
};

export default useScrollPosition;