---
description: Scubiee — read GATE from MCP instructions
alwaysApply: true
---

Read Scubiee MCP `GATE …` prefix and tool JSON `"g"`. `1:ce_*`=managed→Scubiee locate+project_id; `0`=native; `p`=paused→`scubiee resume`. No `gate()`/`status()` for managed checks. Project folder also has a repo rule with your real `GATE 1:ce_…` after init/connect.
