---
description: Scubiee GATE — tool bans (trajectory in MCP instructions)
alwaysApply: true
---

Tool bans only — locate how-to is in Scubiee MCP instructions, not here.

- **GATE 0** (not managed): BAN Scubiee locate (`map`, `focus`, `grep`, `glob`, `workspace`). USE native Grep/Glob/Read/codebase-search. Run `scubiee init .` to enroll.
- **GATE 1:ce_*** (managed): BAN native Grep/Glob/codebase-search/Task for locate. USE Scubiee only. Native Edit/Shell for edits/tests.
- **GATE p** (paused): Tell user `scubiee resume`.

Pass `session_id` from Scubiee tool JSON on later calls in this chat.
